module.exports = {
  theme: {
    textColor: theme => ({
      ...theme('colors'),
      primaryGray: '#443a3a',
    }),
    backgroundColor: theme => ({
      ...theme('colors'),
      primaryGray: '#443a3a',
    }),
  },
};
